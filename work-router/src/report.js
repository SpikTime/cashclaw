function percent(value,total){return total?`${Math.round(value/total*100)}%`:"0%";}
export function generateSourceReport(state,now=new Date()){const since=now.getTime()-24*3600000;const cycles=(state.cycles||[]).filter(x=>new Date(x.startedAt).getTime()>=since);const lines=["# CashClaw Source 24H Report","",`Generated: ${now.toISOString()}`,"","## Source ranking","","| Source | Quality | Messages | Opportunities | Relevant | Spam | Budget | Duplicates |","|---|---:|---:|---:|---:|---:|---:|---:|"];const ranked=[...(state.sources||[])].sort((a,b)=>(b.qualityScore??-1)-(a.qualityScore??-1));for(const source of ranked){const s=source.stats||{};lines.push(`| @${source.username||source.id} | ${source.qualityScore??"unrated"} | ${s.messagesSeen||0} | ${s.opportunitiesDetected||0} | ${percent(s.relevantOpportunities||0,s.messagesSeen||0)} | ${percent(s.spam||0,s.messagesSeen||0)} | ${s.projectsWithBudget||0} | ${s.duplicates||0} |`);}const sum=(key)=>cycles.reduce((n,c)=>n+Number(c[key]||0),0);lines.push("","## Throughput","",`- Cycles: ${cycles.length}`,`- New opportunities: ${sum("newOpportunities")}`,`- Local rejects: ${sum("locallyRejected")}`,`- Duplicates: ${sum("duplicates")}`,`- Cheap analyzed: ${sum("cheapAnalyzed")}`,`- Deep analyzed: ${sum("deepAnalyzed")}`,`- HOT / GOOD / MAYBE: ${sum("HOT")} / ${sum("GOOD")} / ${sum("MAYBE")}`,`- Delivery failures: ${sum("deliveryErrors")}`,`- LLM failures: ${sum("llmErrors")}`,"","## Recommendations","","Review unrated and degraded sources manually. Priority changes are recommendations only and are never applied automatically.","");return lines.join("\n");}
