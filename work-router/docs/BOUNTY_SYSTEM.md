# Bounty system

The bounty record keeps provider, funding status, advertised amount, contributors, solver count, payout conditions and timestamps separate from an opportunity. Money states are `advertised`, `potential`, `claimed`, `approved`, `payment_pending`, `paid`, `failed` and `disputed`.

Only `paid` events with a confirmed payment amount count as revenue.
