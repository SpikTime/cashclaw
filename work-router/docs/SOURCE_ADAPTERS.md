# Source adapters

Each adapter declares an acquisition model and can discover/read details/draft only. Paid submissions require one explicit `spend_approval` for one opportunity and are not bulk-approved.

GitHub uses the official public Issues Search API. Labels and provider links discover candidates but do not prove payment. Opire is supported by manual import only until a documented, permitted public integration is configured.
