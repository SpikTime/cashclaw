# Architecture

Discovery adapters produce untrusted opportunities. Free, bounty, inbound, experimental and paid models are explicit. The router runs a cheap keyword/economics prefilter, then only selected candidates reach Claude. Bounty candidates pass trust, anomaly and feasibility checks before that step.

No source adapter can submit paid proposals or claims. Those are external side effects and are disabled in code by default.
